baseURL = 'https://advika.dev/'
languageCode = 'en-us'
title = 'advika-blog'

[params]
    dateFormat = "Jan 2, 2006"
    ogImage = "/images/cat-favicon.png"

[HTTPCache]
    [HTTPCache.cache]
        [HTTPCache.cache.for]
            excludes = ['**']
            includes = []