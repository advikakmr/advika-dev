baseURL = 'https://advika.dev/'
languageCode = 'en-us'
title = 'advika-blog'

[params]
    dateFormat = "01-02-06"
    ogImage = "/images/mcp_logo.webp"

[HTTPCache]
    [HTTPCache.cache]
        [HTTPCache.cache.for]
            excludes = ['**']
            includes = []